// Generated by CoffeeScript 2.3.2
(function() {
  var fs, original, red;

  fs = require('fs');

  ({red} = require('chalk'));

  original = process.cwd();

  // fileDir = (name) ->
  // fs.stat(name)
  module.exports = function(path) {
    return new Promise(function(resolve, reject) {
      return fs.readdir(path, function(err, dir) {
        if (err) {
          process.chdir(original);
          return reject({
            code: 0,
            err_msg: `${path}目录读取错误`
          });
        } else {
          process.chdir(original);
          return resolve(dir);
        }
      });
    });
  };

}).call(this);

//# sourceMappingURL=currentFiles.js.map
