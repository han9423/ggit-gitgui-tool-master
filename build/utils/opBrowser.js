// Generated by CoffeeScript 2.3.2
(function() {
  var exec, platform, url;

  ({platform} = require('os'));

  ({exec} = require('child_process'));

  url = 'http://localhost';

  module.exports = function(port) {
    var terminal;
    console.log(platform());
    switch (platform()) {
      case 'linux':
        terminal = 'xdg-open';
        break;
      case 'win32':
        terminal = 'start';
        break;
      case 'darwin':
        terminal = 'open';
        break;
      default:
        return 'not support this platform';
    }
    return exec(`${terminal} ${url}:${port}`);
  };

}).call(this);

//# sourceMappingURL=opBrowser.js.map
