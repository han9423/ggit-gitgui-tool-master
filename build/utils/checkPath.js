// Generated by CoffeeScript 2.3.2
(function() {
  var isAbsolute, originalPath;

  ({isAbsolute} = require('path'));

  originalPath = process.cwd();

  module.exports = function(path) {
    return new Promise(function(resolve, reject) {
      var error;
      if (isAbsolute(path)) {
        try {
          process.chdir(path);
        } catch (error1) {
          error = error1;
          reject({
            code: 0,
            err_msg: `${path} 没有这个路径`
          });
        }
        return resolve({
          code: 1
        });
      } else {
        return reject({
          code: 0,
          err_msg: `${path} 不是绝对路径`
        });
      }
    });
  };

}).call(this);

//# sourceMappingURL=checkPath.js.map
